# 端侧大模型实践汇报

各位领导好，我叫唐锐，非常荣幸能够和各位领导汇报我这三个月的实习工作成果。

今天我的汇报将围绕 **端侧大模型推理与量化实践** 这个主题展开，主要分为以下几个部分：

1.  **基础工作：** 首先会介绍项目的基础，包括如何将 `llama.cpp` 框架移植到安卓平台，并实现模型的初步推理。
2.  **性能优化探索：** 接着，我会分享在性能优化方面所做的工作，重点是 **OpenCL GPU加速** 的尝试及其测试结果。
3.  **核心功能实现：** 之后是几个核心功能的实现，包括 **多模态视觉能力** 的支持，以及 **端侧Agent的开发**，这其中又涵盖了从简单的工具路由到复杂的多步推理和模型微调的探索。
4.  **模型量化实践：** 我还会汇报在模型量化方面进行的研究和测试，以及我们是如何在性能和体积之间做出权衡的。
5.  **总结与展望：** 最后，对整个项目进行总结，并分享落地未来的思考。

接下来，我将开始详细介绍第一部分的内容。

## 1. 基础

主要是llamacpp的移植，通过ndk交叉编译成移动端的动态链接库方式实现。

安卓端再通过jni的方式来调用，所以后半个月的工作内容基本就是编译为动态连接库后，学习llamacpp公开的若干api，然后再在移动端开发

主要完成基本的模型加载，管理，调用，ChatML模版化，kv缓存管理，提示词处理和推理功能。开发一个用户交互的界面并集成到iCenter内

针对我开发和测试过程的设备，其编译的abi版本是：arm64 v7a




# 2. openCL支持

在完成基础的llamacpp移植和安卓端推理平台开发后，紧接着做了一个性能优化的探索，开发了bench功能来评估量化模型的推理速度

完成对移动端的GPU支持开发。移动端的GPU支持现在主要是分为openCL和Vulkan这两种方案

我选择了openCL的方案，因为社区里有枭龙的工作人员参与了这部分的开发，迭代速度很快，未来可期。


然后这里是详细的基准测试报告：

测试目的是探究移动端下不同量化模型的GPU加速效果

并在测试后，得到了一个非常反直觉的结果

主流的模型层数大概都在20 - 30层左右，随着offload到gpu的层数越多，模型的bench表现反而越差

这样的反常结果大概率是可能是因为

1. 移动端 GPU 的 OpenCL 驱动优化程度不足 且 cpu-gpu的拷贝速度过慢，实际计算的并行度不够大 导致的
   
综上目前还是维持纯cpu效果更好，端侧的gpu需要等待硬件驱动和底层算子的进一步优化

# 3. 多模态视觉支持

在完成性能优化的探索后

继续调研了移动端的多模态支持

这部分技术方面主要是通过加载mtmd这个第三方库来实现的

llamacpp本身对多模态的支持也是通过两个模型来实现的，一个基础的文本模型+一个专门对图像做embedding的模型

这里我也做了简单的多模态测试报告，所有测试采用的基模都是比较主流的模型或是在leaderboard上排名比较高的模型，再对它们做量化部署到手机执行视觉方面推理

开发过程中也发现了一些问题，采样器设置问题，提示词问题等，未来落地多模态需要进一步打磨



# 4. agent开发

## 4.1 简单功能路由

在完成多模态的研究后继续移动端端侧ai的agent表现

最终目的是开发出支持本地toolCalling和MCP接口的综合agent

这边是简单的一次调用的样例，我对iCenter内其他的一些部件的功能做了抽取，封装成本地的若干个tool

在模型初始化阶段做了提示词工程，规范tool的调用格式和内容。通过注册的方式告知tool的内容。

这个gif图是执行toolcalling的结果

## 4.2 多步推理

但是肯定是不能满足简单功能的路由，还希望模型能够综合处理用户的请求，更智能一点

所以后面又做了链式调用和多步推理的工作

这里是简单的演示，


## 4.3 对于toolCalling的测试

toolCalling这里我也做了简单的测试，采用的量化模型也都是比较主流和排名较高的，执行任务是日程创建和查询。

并且全都是 zero-shot 的场景

实际上到了这一步，即使简单的路由功能，对很多模型来说也是严重的挑战

像Gemma3和smol这两个小模型已经完全无法工作了

只有稍大的模型像qwen1.7B这样的，才能够比较好的执行命令

还有要说明的是，推理模型开启推理的情况下执行成功率是显著优于未开启推理的模式


然后下面是简单路由任务的一个bench测试


最好的还是qwen1.7B开启推理模式的情况，但是执行时间过长，基本不可接受，并且多模态和功能路由混合的情况下会出现优先级的判定错误问题


## 4.4 微调270m

为了应对一次功能路由的场景，探索了谷歌最新推出的270m的高速小模型，该模型非常轻量，微调成本极低

非常适合拿来在这个业务下做路由功能

下面是我一个简单的测试

首先是270m在专业场景情况下few-shot和微调的表现

在强专业的领域，270m还是无法胜任的


随后我又将270m用我们这个业务数据进行了微调，数据来源我创建的10个工具，我用ai组合了几十种场景，最后生成了1w条样本

这边微调也能看到成本很低，基本在200多步的时候就收敛了

最后是一个评估

## 5. 量化

最后是我的模型量化报告，精度越高的模型占用内存比较高，这块是希望在保持智能的情况下尽可能的降低内存占用


## 6. 链式调用+多步推理

最下面的是还在研究的链式调用和多步推理，

一次路由处理的功能较为简单，这部分主要任务是探索端侧ai对高级agent的构建支持能力

但是目前还存在不小阻力

这边是做多步推理的架构和工作流，以及提示词工程的核心工作流




当然目前还存在不少问题，最基础的模型输出格式的问题，这部分单次路由测试那块有涉及

还有就是小模型的分析用户问题，分析前置要求的能力比较差

由于GPU限制，这里使用的都是基模，没有根据场景微调过，只在初始阶段做了5-shot的上下文学习



最后是实验过程使用的tool和pp阶段提示词

目前这块的研究方向一个是探索基模的智力极限，通过提示此工程和下面的优化来提高成功率，第二个是针对特定任务做对应的领域微调，比如做日程相关任务就给大量对应场景微调注入相关知识。同时还应该持续关注其它推理框架或sdk如onnx  google的edge sdk，看是否有更好的解决方案

# 7. 未来应用展望

我觉得端侧模型的应用场景，目前的价值在以下几个场景

   一个是端侧ai更智能、更私密：不需要联网。它能直接在你的手机上帮你安排日程、回复消息、甚至总结文章。因为所有计算都在本地完成，所以响应会非常快，而且个人数据，比如聊天记录和照片不用上传到网络，可以保障隐私。

   另一个是多模态能力，借助多模态视觉，可以用摄像头对准任何东西。比如，在国外旅行时，对准菜单就能实时翻译；看到不认识的植物，拍一下就知道它是什么

    最后是端侧ai整合iCenter，模型和iCenter的若干功能紧密结合，比如语音和ai交互让他创建日程，调整字体，清理缓存等

    这边整理了一些未来可期的应用场景

# 8. 总结

我的汇报到此结束，谢谢大家！